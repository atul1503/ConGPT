{
  "name": "congpt",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace for the ConGPT app",
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "serve": "npm run build --prefix frontend && node backend/index.js",
    "start": "npm start --prefix backend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix backend || true",
    "test": "npm test --prefix backend || true"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED"
}
